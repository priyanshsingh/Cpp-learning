#include <iostream>
using namespace std;

void vol(a)
{
    float volume = a * a * a;
    cout << "Volume of cube " << i << " is = " << volume;
}

int main()
{
    int n;
    cout << "How many times you want the volume to be found = ";
    cin >> a1;

    for (int i = 1, i <= n, i++)
    {
        vol(i);
    }

    cout << endl;
    return 0;
}